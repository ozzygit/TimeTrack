<Window x:Class="TimeTrack.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:data="clr-namespace:TimeTrack.Data"
        xmlns:views="clr-namespace:TimeTrack.Views"
        xmlns:viewModels="clr-namespace:TimeTrack.ViewModels"
        xmlns:converters="clr-namespace:TimeTrack.Converters"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=viewModels:TimeKeeperViewModel, IsDesignTimeCreatable=False}"
        Title="TimeTrack v3" Height="675" Width="800" MinWidth="700" MinHeight="375"
        Icon="/Resources/time.png" Background="{DynamicResource WindowBackground}"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <data:TimeEntryUIConverter x:Key="TimeEntryUIConverter"/>
        <converters:AllNotEmptyConverter x:Key="AllNotEmptyConverter"/>
        <converters:ShortcutDisplayConverter x:Key="ShortcutDisplayConverter"/>


        <!-- Modern Menu Styles -->
        <Style TargetType="Menu">
            <Setter Property="Background" Value="{DynamicResource BackgroundBrush}"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>

        <Style TargetType="MenuItem">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Border x:Name="Border" 
                                Background="{TemplateBinding Background}"
                                BorderThickness="0"
                                CornerRadius="4"
                                Margin="2,0">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" SharedSizeGroup="Icon"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto" SharedSizeGroup="Shortcut"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Icon (for submenus if needed) -->
                                <ContentPresenter x:Name="Icon"
                                                Grid.Column="0"
                                                Margin="6,0,6,0"
                                                VerticalAlignment="Center"
                                                ContentSource="Icon"/>

                                <!-- Header Text -->
                                <ContentPresenter x:Name="HeaderHost"
                                                Grid.Column="1"
                                                ContentSource="Header"
                                                Margin="{TemplateBinding Padding}"
                                                VerticalAlignment="Center"
                                                RecognizesAccessKey="True"/>

                                <!-- Input Gesture Text (Keyboard Shortcut) -->
                                <TextBlock x:Name="InputGestureText"
                                         Grid.Column="2"
                                         Text="{TemplateBinding InputGestureText}"
                                         Margin="20,0,10,0"
                                         VerticalAlignment="Center"
                                         Foreground="{DynamicResource SubtleBrush}"
                                         FontSize="12"/>

                                <!-- Submenu Arrow -->
                                <Path x:Name="ArrowPath"
                                    Grid.Column="3"
                                    Margin="4,0,6,0"
                                    VerticalAlignment="Center"
                                    Fill="{DynamicResource TextBrush}"
                                    Data="M 0 0 L 4 4 L 0 8 Z"
                                    Visibility="Collapsed"/>

                                <!-- Popup for submenus -->
                                <Popup x:Name="PART_Popup"
                                     AllowsTransparency="True"
                                     Placement="Bottom"
                                     IsOpen="{TemplateBinding IsSubmenuOpen}"
                                     Focusable="False"
                                     PopupAnimation="Fade"
                                     VerticalOffset="2">
                                    <Border Background="{DynamicResource BackgroundBrush}"
                                          BorderBrush="{DynamicResource BorderBrush}"
                                          BorderThickness="1"
                                          CornerRadius="6"
                                          Padding="4">
                                        <Border.Effect>
                                            <DropShadowEffect Color="#000000" Opacity="0.15" 
                                                            BlurRadius="12" ShadowDepth="4" Direction="270"/>
                                        </Border.Effect>
                                        <ScrollViewer Style="{DynamicResource {ComponentResourceKey 
                                                      TypeInTargetAssembly={x:Type FrameworkElement}, 
                                                      ResourceId=MenuScrollViewer}}"
                                                    MaxHeight="400">
                                            <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Cycle"
                                                          Grid.IsSharedSizeScope="True"
                                                          Margin="0,4"/>
                                        </ScrollViewer>
                                    </Border>
                                </Popup>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <!-- Top level menu item (File, Edit, etc.) -->
                            <Trigger Property="Role" Value="TopLevelHeader">
                                <Setter TargetName="PART_Popup" Property="Placement" Value="Bottom"/>
                                <Setter TargetName="ArrowPath" Property="Visibility" Value="Collapsed"/>
                            </Trigger>

                            <!-- Submenu header (has children) -->
                            <Trigger Property="Role" Value="SubmenuHeader">
                                <Setter TargetName="PART_Popup" Property="Placement" Value="Right"/>
                                <Setter TargetName="ArrowPath" Property="Visibility" Value="Visible"/>
                            </Trigger>

                            <!-- Mouse over effect -->
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource ButtonHoverBrush}"/>
                            </Trigger>
                            <!-- Pressed effect -->
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource ButtonPressedBrush}"/>
                            </Trigger>

                            <!-- Submenu open effect for top level -->
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsSubmenuOpen" Value="True"/>
                                    <Condition Property="Role" Value="TopLevelHeader"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource ButtonPressedBrush}"/>
                            </MultiTrigger>

                            <!-- Disabled state -->
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Separator Style -->
        <Style TargetType="Separator">
            <Setter Property="Background" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="Height" Value="1"/>
            <Setter Property="Margin" Value="8,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Separator">
                        <Border Background="{DynamicResource BorderBrush}" 
                              Height="1" 
                              Margin="32,4,8,4"
                              CornerRadius="0.5"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern Button Style -->
        <Style TargetType="Button">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Background" Value="{DynamicResource BackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource ButtonHoverBrush}"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource ButtonHoverBorderBrush}"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="{DynamicResource ButtonPressedBrush}"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.5"/>
                    <Setter Property="Cursor" Value="Arrow"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Primary Button Style -->
        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{DynamicResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource PrimaryHoverBrush}"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="{DynamicResource PrimaryPressedBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Modern TextBlock Style -->
        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
        </Style>

        <!-- Label Style -->
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>

        <!-- Modern TextBox Style -->
        <Style TargetType="TextBox">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="4,3"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="{DynamicResource BackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                        Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="{DynamicResource TextBoxHoverBorderBrush}"/>
                </Trigger>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Modern DataGrid Style -->
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="{DynamicResource BackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource DataGridGridLineBrush}"/>
            <Setter Property="RowBackground" Value="{DynamicResource BackgroundBrush}"/>
            <Setter Property="AlternatingRowBackground" Value="{DynamicResource DataGridAltRowBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Background" Value="{DynamicResource DataGridHeaderBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="Padding" Value="8,8"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
        </Style>

        <Style TargetType="DataGridRow">
            <EventSetter Event="MouseDoubleClick" Handler="DgRow_MouseDoubleClick"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Style.Triggers>
                <!-- MouseOver on unselected rows only - must come BEFORE IsSelected -->
                <MultiTrigger>
                    <MultiTrigger.Conditions>
                        <Condition Property="IsMouseOver" Value="True"/>
                        <Condition Property="IsSelected" Value="False"/>
                    </MultiTrigger.Conditions>
                    <Setter Property="Background" Value="{DynamicResource DataGridRowHoverBrush}"/>
                </MultiTrigger>
                <!-- Selected state - AFTER MouseOver so it takes precedence -->
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{DynamicResource DataGridRowSelectedBrush}"/>
                    <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Override DataGridCell style to prevent cell borders from showing -->
        <Style TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Style.Triggers>
                <!-- Keep cell background transparent when row is selected -->
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
                    <Setter Property="BorderBrush" Value="Transparent"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Modern DatePicker Style -->
        <Style TargetType="DatePicker">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <!-- Modern CheckBox Style -->
        <Style TargetType="CheckBox">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
        </Style>

        <!-- Modern DataGrid CheckBox Style -->
        <Style x:Key="DataGridCheckBoxStyle" TargetType="CheckBox">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid Background="Transparent">
                            <Border x:Name="CheckBoxBorder"
                                    Width="18"
                                    Height="18"
                                    Background="{DynamicResource BackgroundBrush}"
                                    BorderBrush="{DynamicResource BorderBrush}"
                                    BorderThickness="2"
                                    CornerRadius="3">
                                <Path x:Name="CheckMark"
                                      Width="12"
                                      Height="12"
                                      Data="M 0,5 L 4,9 L 10,1"
                                      Stroke="{DynamicResource PrimaryBrush}"
                                      StrokeThickness="2.5"
                                      StrokeLineJoin="Round"
                                      Visibility="Collapsed"
                                      Stretch="Uniform"
                                      Margin="2"/>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <!-- Checked State -->
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="CheckBoxBorder" Property="Background" Value="{DynamicResource CheckBoxCheckedBrush}"/>
                                <Setter TargetName="CheckBoxBorder" Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                            </Trigger>

                            <!-- Mouse Over -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CheckBoxBorder" Property="BorderBrush" Value="{DynamicResource PrimaryHoverBrush}"/>
                                <Setter TargetName="CheckBoxBorder" Property="Background" Value="{DynamicResource CheckBoxHoverBrush}"/>
                            </Trigger>

                            <!-- Mouse Over + Checked -->
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                    <Condition Property="IsChecked" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="CheckBoxBorder" Property="Background" Value="{DynamicResource CheckBoxHoverCheckedBrush}"/>
                                <Setter TargetName="CheckBoxBorder" Property="BorderBrush" Value="{DynamicResource PrimaryHoverBrush}"/>
                            </MultiTrigger>

                            <!-- Disabled -->
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="CheckBoxBorder" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Card Style for Sections -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource BackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" Opacity="0.05" 
                                    BlurRadius="8" ShadowDepth="2" Direction="270"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Notes Cell Text Style -->
        <Style x:Key="NotesCellTextStyle" TargetType="TextBlock">
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
            <Setter Property="TextWrapping" Value="NoWrap"/>
            <Setter Property="MaxHeight" Value="20"/>
            <Setter Property="ToolTip" Value="{Binding Notes}"/>
        </Style>

        <!-- Tab strip: label button (left portion of each tab) -->
        <Style x:Key="TabButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="28"/>
            <Setter Property="MinWidth" Value="60"/>
            <Setter Property="MaxWidth" Value="120"/>
            <Setter Property="Padding" Value="8,0"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="{DynamicResource TabInactiveBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4,0,0,4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsActive}" Value="True">
                    <Setter Property="Background" Value="{DynamicResource PrimaryBrush}"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                </DataTrigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource TabHoverBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Tab strip: close button (right portion of each tab) -->
        <Style x:Key="TabCloseButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="22"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="{DynamicResource TabInactiveBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource SubtleBrush}"/>
            <Setter Property="BorderThickness" Value="0,1,1,1"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="0,4,4,0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsActive}" Value="True">
                    <Setter Property="Background" Value="{DynamicResource PrimaryHoverBrush}"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                </DataTrigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#D32F2F"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="BorderBrush" Value="#D32F2F"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>

    <!-- Keyboard bindings are configured dynamically in MainWindow.xaml.cs via ApplyKeyboardShortcuts() -->
    <Window.InputBindings>
    </Window.InputBindings>

    <DockPanel>
        <!--Menu Bar-->
        <Menu DockPanel.Dock="Top" Height="40" Padding="8,4" 
              Background="{DynamicResource BackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1">
            <MenuItem Header="_File">
                <MenuItem Header="E_xit" Click="MenuExit_Click" InputGestureText="Alt+F4"/>
            </MenuItem>
            <MenuItem Header="_Edit">
                <MenuItem x:Name="SubmitMenuItem" Header="_Submit Entry" Click="BtnSubmit"
                          InputGestureText="{Binding Path=., Mode=OneWay, Converter={StaticResource ShortcutDisplayConverter}, ConverterParameter=Submit}"/>
                <MenuItem x:Name="InsertMenuItem" Header="_Insert Record"
                          Command="{x:Static views:MainWindow.InsertCommand}"
                          InputGestureText="{Binding Path=., Mode=OneWay, Converter={StaticResource ShortcutDisplayConverter}, ConverterParameter=Insert}"/>
                <MenuItem x:Name="DeleteMenuItem" Header="_Delete Selected"
                          Command="{x:Static views:MainWindow.DeleteCommand}"
                          InputGestureText="{Binding Path=., Mode=OneWay, Converter={StaticResource ShortcutDisplayConverter}, ConverterParameter=Delete}"/>
                <Separator/>
                <MenuItem x:Name="SelectAllMenuItem" Header="_Select All" Click="BtnSelectAll"
                          InputGestureText="{Binding Path=., Mode=OneWay, Converter={StaticResource ShortcutDisplayConverter}, ConverterParameter=SelectAll}"/>
            </MenuItem>
            <MenuItem Header="_Navigation">
                <MenuItem x:Name="TodayMenuItem" Header="_Today"
                          Command="{x:Static views:MainWindow.TodayCommand}"
                          InputGestureText="{Binding Path=., Mode=OneWay, Converter={StaticResource ShortcutDisplayConverter}, ConverterParameter=Today}"/>
                <MenuItem x:Name="PrevDayMenuItem" Header="_Previous Day"
                          Command="{x:Static views:MainWindow.PrevDayCommand}"
                          InputGestureText="{Binding Path=., Mode=OneWay, Converter={StaticResource ShortcutDisplayConverter}, ConverterParameter=PrevDay}"/>
                <MenuItem x:Name="NextDayMenuItem" Header="_Next Day"
                          Command="{x:Static views:MainWindow.NextDayCommand}"
                          InputGestureText="{Binding Path=., Mode=OneWay, Converter={StaticResource ShortcutDisplayConverter}, ConverterParameter=NextDay}"/>
            </MenuItem>
            <MenuItem Header="_Tools">
                <MenuItem x:Name="OptionsMenuItem" Header="_Options"
                          Command="{x:Static views:MainWindow.OptionsCommand}"
                          InputGestureText="{Binding Path=., Mode=OneWay, Converter={StaticResource ShortcutDisplayConverter}, ConverterParameter=Options}"/>
            </MenuItem>
            <MenuItem Header="_Help">
                <MenuItem x:Name="AboutMenuItem" Header="_About TimeTrack v3"
                          Command="{x:Static views:MainWindow.HelpCommand}"
                          InputGestureText="{Binding Path=., Mode=OneWay, Converter={StaticResource ShortcutDisplayConverter}, ConverterParameter=About}"/>
            </MenuItem>
        </Menu>

        <!-- Tab Bar -->
        <Border DockPanel.Dock="Top" Background="{DynamicResource BackgroundBrush}"
                BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1"
                Padding="12,5,12,5">
            <ScrollViewer HorizontalScrollBarVisibility="Auto"
                          VerticalScrollBarVisibility="Disabled"
                          PanningMode="HorizontalOnly">
                <StackPanel Orientation="Horizontal">
                    <!-- Permanent Main tab -->
                    <Button Height="28" Margin="0,0,2,0" Padding="12,0" FontSize="12" Cursor="Hand"
                            Click="BtnMainTab" ToolTip="Main — entries log and date navigation">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="{DynamicResource TabInactiveBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
                                <Setter Property="BorderThickness" Value="1"/>
                                <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    CornerRadius="4">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                                                  Margin="{TemplateBinding Padding}"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsMainTabFocused}" Value="True">
                                        <Setter Property="Background" Value="{DynamicResource PrimaryBrush}"/>
                                        <Setter Property="Foreground" Value="White"/>
                                        <Setter Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                                    </DataTrigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="{DynamicResource TabHoverBrush}"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <TextBlock Text="Main"/>
                    </Button>
                    <Border Width="1" Background="{DynamicResource BorderBrush}" Margin="4,2,6,2"/>
                    <ItemsControl ItemsSource="{Binding OpenEntries}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Margin="0,0,2,0">
                                    <Button Style="{StaticResource TabButtonStyle}"
                                            Tag="{Binding}" Click="BtnFocusTab"
                                            ToolTip="{Binding TicketDisplay}">
                                        <TextBlock Text="{Binding TabDisplay}"
                                                   TextTrimming="CharacterEllipsis"
                                                   MaxWidth="140"/>
                                    </Button>
                                    <Button Style="{StaticResource TabCloseButtonStyle}"
                                            Tag="{Binding}" Click="BtnCloseTab"
                                            ToolTip="Close this entry">✕</Button>
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <Button Width="28" Height="28" Content="+" FontSize="14"
                            FontWeight="Bold" Padding="0" Margin="6,0,0,0"
                            Click="BtnNewEntry"
                            ToolTip="Start a new open entry"/>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!--Status Bar - MOVED OUTSIDE ScrollViewer-->
        <StatusBar DockPanel.Dock="Bottom" Height="25">
            <StatusBarItem>
                <TextBlock x:Name="StatusText" Text="Ready" />
            </StatusBarItem>
        </StatusBar>

        <!-- Main Content Area (two-panel switcher) -->
        <Grid>
            <!-- MAIN TAB PANEL -->
            <Grid Visibility="{Binding IsMainTabFocused, Converter={StaticResource BoolToVis}}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Date nav + inline stats -->
                <Border Grid.Row="0" Style="{StaticResource CardStyle}" Margin="12,12,12,8" Padding="12,10">
                    <StackPanel>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" FontWeight="SemiBold" FontSize="14"
                                     VerticalAlignment="Center" Margin="0,0,8,0" Text="Date:"/>
                            <TextBlock Name="txtCurrentDate" Grid.Column="1" VerticalAlignment="Center"
                                     Margin="0,0,12,0" FontSize="14" Text="{Binding CurrentDate}"/>
                            <DatePicker Name="CalDate" Grid.Column="2" Width="140"
                                      HorizontalAlignment="Left" Margin="0,0,12,0"
                                      SelectedDate="{Binding Date}" SelectedDateChanged="CalLoadDate"
                                      ToolTip="Select a date to view entries"/>
                            <Button Name="BtnBack" Grid.Column="3" Width="32" Height="32"
                                  Margin="0,0,4,0" Content="←" FontSize="16"
                                  Click="BtnGoBack" ClickMode="Release"
                                  ToolTip="Go to previous day (Ctrl+Left)"/>
                            <Button Name="BtnToday" Grid.Column="4" Height="32" Padding="12,0"
                                  Margin="0,0,4,0" Content="Today"
                                  Click="BtnGotoToday" ClickMode="Release" IsEnabled="False"
                                  ToolTip="Return to today's date (Ctrl+T)"/>
                            <Button Name="BtnForward" Grid.Column="5" Width="32" Height="32"
                                  Content="→" FontSize="16"
                                  Click="BtnGoForward" ClickMode="Release"
                                  ToolTip="Go to next day (Ctrl+Right)"/>
                        </Grid>
                        <!-- Inline stats row -->
                        <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                            <TextBlock FontSize="12" VerticalAlignment="Center">
                                <Run Text="Total: " Foreground="{DynamicResource SubtleBrush}"/>
                                <Run Text="{Binding HoursTotal}" FontWeight="SemiBold"/>
                                <Run Text="h" Foreground="{DynamicResource SubtleBrush}"/>
                            </TextBlock>
                            <TextBlock Text="  ·  " FontSize="12" Foreground="{DynamicResource SubtleBrush}" VerticalAlignment="Center"/>
                            <TextBlock FontSize="12" VerticalAlignment="Center">
                                <Run Text="Gaps: " Foreground="{DynamicResource SubtleBrush}"/>
                                <Run Text="{Binding GapsTotal}" FontWeight="SemiBold"/>
                                <Run Text=" min" Foreground="{DynamicResource SubtleBrush}"/>
                            </TextBlock>
                            <TextBlock Text="  ·  " FontSize="12" Foreground="{DynamicResource SubtleBrush}" VerticalAlignment="Center"/>
                            <TextBlock FontSize="12" VerticalAlignment="Center">
                                <Run Text="Billable: " Foreground="{DynamicResource SubtleBrush}"/>
                                <Run Text="{Binding BillableUnits}" FontWeight="SemiBold"/>
                                <Run Text=" units" Foreground="{DynamicResource SubtleBrush}"/>
                            </TextBlock>
                            <TextBlock Text="  ·  " FontSize="12" Foreground="{DynamicResource SubtleBrush}" VerticalAlignment="Center"/>
                            <TextBlock FontSize="12" VerticalAlignment="Center">
                                <Run Text="Selected: " Foreground="{DynamicResource SubtleBrush}"/>
                                <Run Text="{Binding SelectedHours}" FontWeight="SemiBold"/>
                                <Run Text="h " Foreground="{DynamicResource SubtleBrush}"/>
                                <Run Text="{Binding SelectedMins}" FontWeight="SemiBold"/>
                                <Run Text="m" Foreground="{DynamicResource SubtleBrush}"/>
                            </TextBlock>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- DataGrid -->
                <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="12,0,12,8" Padding="12">
                    <DataGrid Name="DgTimeRecords"
                             ItemsSource="{Binding Entries}"
                             SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                             AutoGenerateColumns="False"
                             HeadersVisibility="Column"
                             ColumnWidth="Auto"
                             RowHeaderWidth="0"
                             MinColumnWidth="40"
                             IsReadOnly="True"
                             EnableRowVirtualization="True"
                             EnableColumnVirtualization="True"
                             ScrollViewer.CanContentScroll="True"
                             VirtualizingPanel.IsVirtualizing="True"
                             VirtualizingPanel.VirtualizationMode="Recycling"
                             HorizontalScrollBarVisibility="Disabled"
                             SelectionChanged="DgTimeRecords_SelectionChanged"
                             PreviewMouseRightButtonDown="DgTimeRecords_PreviewMouseRightButtonDown">
                        <DataGrid.InputBindings>
                            <KeyBinding Key="Delete" Command="{x:Static views:MainWindow.DeleteCommand}"/>
                            <KeyBinding Key="A" Modifiers="Control" Command="{x:Static views:MainWindow.SelectAllCommand}"/>
                        </DataGrid.InputBindings>
                        <DataGrid.Columns>
                            <DataGridCheckBoxColumn Width="30"
                                Binding="{Binding Recorded, UpdateSourceTrigger=PropertyChanged}"
                                ElementStyle="{StaticResource DataGridCheckBoxStyle}"
                                EditingElementStyle="{StaticResource DataGridCheckBoxStyle}">
                                <DataGridCheckBoxColumn.Header>
                                    <CheckBox x:Name="ChkSelectAllHeader"
                                              Click="ChkSelectAllHeader_Click"
                                              ToolTip="Select all (Ctrl+A)"
                                              HorizontalAlignment="Center"/>
                                </DataGridCheckBoxColumn.Header>
                            </DataGridCheckBoxColumn>
                            <DataGridTextColumn Header="Start" Width="82"
                                Binding="{Binding StartTime, Converter={StaticResource TimeEntryUIConverter}}"/>
                            <DataGridTextColumn Header="End" Width="82"
                                Binding="{Binding EndTime, Converter={StaticResource TimeEntryUIConverter}}"/>
                            <DataGridTextColumn Header="Units" Width="52" Binding="{Binding BillableUnits}">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Right"/>
                                        <Setter Property="Margin" Value="0,0,8,0"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Ticket Number" Width="140"
                                Binding="{Binding TicketNumber}"/>
                            <DataGridTextColumn Header="Notes" Width="*" Binding="{Binding Notes}">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock" BasedOn="{StaticResource NotesCellTextStyle}"/>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                        <DataGrid.ContextMenu>
                            <ContextMenu Background="{DynamicResource ContextMenuBackground}" BorderBrush="{DynamicResource BorderBrush}"
                                         BorderThickness="1" Padding="0,4"
                                         FontFamily="Segoe UI" FontSize="13">
                                <ContextMenu.Template>
                                    <ControlTemplate TargetType="ContextMenu">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                Padding="{TemplateBinding Padding}"
                                                SnapsToDevicePixels="True">
                                            <Border.Effect>
                                                <DropShadowEffect Color="#000000" Opacity="0.12"
                                                                  BlurRadius="8" ShadowDepth="2" Direction="270"/>
                                            </Border.Effect>
                                            <ItemsPresenter SnapsToDevicePixels="True"/>
                                        </Border>
                                    </ControlTemplate>
                                </ContextMenu.Template>
                                <ContextMenu.Resources>
                                    <Style TargetType="MenuItem">
                                        <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
                                        <Setter Property="Padding" Value="0"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="MenuItem">
                                                    <Border x:Name="Bd" Background="Transparent"
                                                            Padding="14,6">
                                                        <ContentPresenter ContentSource="Header"/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsHighlighted" Value="True">
                                                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ContextMenuItemHoverBrush}"/>
                                                            <Setter Property="Foreground" Value="{DynamicResource PrimaryBrush}"/>
                                                        </Trigger>
                                                        <Trigger Property="IsEnabled" Value="False">
                                                            <Setter Property="Foreground" Value="{DynamicResource SubtleBrush}"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                    <Style TargetType="Separator">
                                        <Setter Property="Margin" Value="10,3"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Separator">
                                                    <Rectangle Height="1" Fill="{DynamicResource BorderBrush}"/>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ContextMenu.Resources>
                                <MenuItem Header="Insert Record"
                                          Command="{x:Static views:MainWindow.InsertCommand}"
                                          CommandTarget="{Binding PlacementTarget, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                <Separator/>
                                <MenuItem Header="Delete Selected"
                                          Command="{x:Static views:MainWindow.DeleteCommand}"
                                          CommandTarget="{Binding PlacementTarget, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                <Separator/>
                                <MenuItem Header="Select All"
                                          Command="{x:Static views:MainWindow.SelectAllCommand}"
                                          CommandTarget="{Binding PlacementTarget, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                            </ContextMenu>
                        </DataGrid.ContextMenu>
                    </DataGrid>
                </Border>

            </Grid>

            <!-- ENTRY TAB PANEL -->
            <Grid VerticalAlignment="Top">
                <Grid.Style>
                    <Style TargetType="Grid">
                        <Setter Property="Visibility" Value="Visible"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsMainTabFocused}" Value="True">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>

                <Border Style="{StaticResource CardStyle}" Margin="12" Padding="16">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Fields row: horizontal layout -->
                        <Grid Grid.Row="0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="155"/>
                                <ColumnDefinition Width="12"/>
                                <ColumnDefinition Width="155"/>
                                <ColumnDefinition Width="12"/>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Start Time -->
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Start Time" Style="{StaticResource LabelStyle}"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="2"/>
                                        <ColumnDefinition Width="28"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBox Name="FldStartTime" Grid.Column="0"
                                             Text="{Binding StartTimeField, UpdateSourceTrigger=PropertyChanged}"
                                             GotFocus="InputField_GotFocus"
                                             LostFocus="TimeField_LostFocus"
                                             PreviewKeyDown="TimeField_PreviewKeyDown"
                                             TabIndex="0"/>
                                    <Button Name="BtnStartTimePicker" Grid.Column="2"
                                            Width="28" Content="🕒" FontSize="12" Padding="0"
                                            Click="BtnStartTimePicker_Click"
                                            ToolTip="Select start time from picker"/>
                                    <Popup Name="PopupStartTime"
                                           PlacementTarget="{Binding ElementName=BtnStartTimePicker}"
                                           Placement="Bottom" StaysOpen="False" AllowsTransparency="True">
                                        <Border Background="{DynamicResource BackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}"
                                                BorderThickness="1" CornerRadius="6" Padding="8">
                                            <Border.Effect>
                                                <DropShadowEffect Color="#000000" Opacity="0.15" BlurRadius="12" ShadowDepth="4" Direction="270"/>
                                            </Border.Effect>
                                            <StackPanel>
                                                <TextBlock Text="Select Start Time" FontWeight="SemiBold" Margin="0,0,0,8"/>
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="8"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="8"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <ComboBox Name="CmbStartHour" Grid.Column="0" Width="60" SelectedIndex="8"/>
                                                    <TextBlock Grid.Column="1" Text=":" VerticalAlignment="Center" FontSize="16" FontWeight="Bold"/>
                                                    <ComboBox Name="CmbStartMinute" Grid.Column="2" Width="60" SelectedIndex="0"/>
                                                    <ComboBox Name="CmbStartPeriod" Grid.Column="4" Width="60" SelectedIndex="0"/>
                                                </Grid>
                                                <Button Content="Set Time" Margin="0,8,0,0"
                                                        Click="BtnSetStartTime_Click"
                                                        Style="{StaticResource PrimaryButton}"/>
                                            </StackPanel>
                                        </Border>
                                    </Popup>
                                </Grid>
                            </StackPanel>

                            <!-- End Time -->
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Finish Time" Style="{StaticResource LabelStyle}"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="2"/>
                                        <ColumnDefinition Width="28"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBox Name="FldEndTime" Grid.Column="0"
                                             Text="{Binding EndTimeField, UpdateSourceTrigger=PropertyChanged}"
                                             GotFocus="InputField_GotFocus"
                                             LostFocus="TimeField_LostFocus"
                                             PreviewKeyDown="TimeField_PreviewKeyDown"
                                             TabIndex="1"/>
                                    <Button Name="BtnEndTimePicker" Grid.Column="2"
                                            Width="28" Content="🕒" FontSize="12" Padding="0"
                                            Click="BtnEndTimePicker_Click"
                                            ToolTip="Select end time from picker"/>
                                    <Popup Name="PopupEndTime"
                                           PlacementTarget="{Binding ElementName=BtnEndTimePicker}"
                                           Placement="Bottom" StaysOpen="False" AllowsTransparency="True">
                                        <Border Background="{DynamicResource BackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}"
                                                BorderThickness="1" CornerRadius="6" Padding="8">
                                            <Border.Effect>
                                                <DropShadowEffect Color="#000000" Opacity="0.15" BlurRadius="12" ShadowDepth="4" Direction="270"/>
                                            </Border.Effect>
                                            <StackPanel>
                                                <TextBlock Text="Select End Time" FontWeight="SemiBold" Margin="0,0,0,8"/>
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="8"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="8"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <ComboBox Name="CmbEndHour" Grid.Column="0" Width="60" SelectedIndex="8"/>
                                                    <TextBlock Grid.Column="1" Text=":" VerticalAlignment="Center" FontSize="16" FontWeight="Bold"/>
                                                    <ComboBox Name="CmbEndMinute" Grid.Column="2" Width="60" SelectedIndex="0"/>
                                                    <ComboBox Name="CmbEndPeriod" Grid.Column="4" Width="60" SelectedIndex="0"/>
                                                </Grid>
                                                <Button Content="Set Time" Margin="0,8,0,0"
                                                        Click="BtnSetEndTime_Click"
                                                        Style="{StaticResource PrimaryButton}"/>
                                            </StackPanel>
                                        </Border>
                                    </Popup>
                                </Grid>
                            </StackPanel>

                            <!-- Ticket Number -->
                            <StackPanel Grid.Column="4">
                                <TextBlock Text="Ticket Number" Style="{StaticResource LabelStyle}"/>
                                <TextBox Name="FldTicketNumber"
                                         Text="{Binding TicketNumberField, UpdateSourceTrigger=PropertyChanged}"
                                         GotFocus="InputField_GotFocus"
                                         TabIndex="2"/>
                            </StackPanel>

                        </Grid>

                        <!-- Notes row: full width -->
                        <Grid Grid.Row="1" Margin="0,12,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="2"/>
                                <ColumnDefinition Width="28"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Notes" Style="{StaticResource LabelStyle}"/>
                                <TextBox Name="FldNotes"
                                         Text="{Binding NotesField, UpdateSourceTrigger=PropertyChanged}"
                                         GotFocus="InputField_GotFocus"
                                         TextWrapping="Wrap"
                                         Height="180"
                                         AcceptsReturn="True"
                                         SpellCheck.IsEnabled="True"
                                         VerticalScrollBarVisibility="Auto"
                                         TabIndex="3"/>
                                <TextBlock Text="{Binding NotesCharacterCount}"
                                           FontSize="11"
                                           Foreground="{DynamicResource SubtleBrush}"
                                           HorizontalAlignment="Right"
                                           Margin="0,3,0,0"/>
                            </StackPanel>
                            <Button Name="BtnNotesPopOut" Grid.Column="2"
                                    Width="28" Height="180" VerticalAlignment="Bottom"
                                    Content="↗" FontSize="12" Padding="0"
                                    Click="BtnNotesPopOut_Click"
                                    ToolTip="Pop out notes editor"/>
                        </Grid>

                        <!-- Bottom row: duration · lunch · submit -->
                        <Grid Grid.Row="2" Margin="0,14,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="16"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0"
                                       Text="{Binding EntryDurationDisplay}"
                                       FontSize="14" FontWeight="SemiBold"
                                       Foreground="{DynamicResource PrimaryBrush}"
                                       VerticalAlignment="Center"/>

                            <CheckBox Name="ChkLunch" Grid.Column="2"
                                    Content="Lunch Break"
                                    Checked="ChkLunch_Checked" Unchecked="ChkLunch_Unchecked"
                                    DataContext="{x:Null}"
                                    VerticalAlignment="Center"
                                    ToolTip="Automatically create a 1-hour lunch break entry"/>

                            <Button Name="BtnSub" Grid.Column="4" Height="36" MinWidth="130"
                                  Style="{StaticResource PrimaryButton}"
                                  TabIndex="4"
                                  Command="{x:Static views:MainWindow.SubmitCommand}">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="Submit Entry" VerticalAlignment="Center"/>
                                    <TextBlock VerticalAlignment="Center" FontSize="11" FontWeight="Normal"
                                             Margin="8,0,0,0" Opacity="0.8"
                                             Text="{Binding Path=., Mode=OneWay, Converter={StaticResource ShortcutDisplayConverter}, ConverterParameter=Submit}"/>
                                </StackPanel>
                                <Button.ToolTip>
                                    <TextBlock>
                                        <Run Text="Add a new time entry to the current day ("/>
                                        <Run Text="{Binding Path=., Mode=OneWay, Converter={StaticResource ShortcutDisplayConverter}, ConverterParameter=Submit}"/>
                                        <Run Text=")"/>
                                    </TextBlock>
                                </Button.ToolTip>
                            </Button>
                        </Grid>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </DockPanel>
</Window>

