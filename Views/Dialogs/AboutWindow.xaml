<Window x:Class="TimeTrack.Views.Dialogs.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="About TimeTrack v2" 
        Height="380" 
        Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F5F5">
    
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Foreground" Value="#323130"/>
        </Style>
    </Window.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Title -->
        <TextBlock Grid.Row="0" 
                   Text="TimeTrack v2" 
                   FontSize="24" 
                   FontWeight="SemiBold"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,10"/>
        
        <!-- Version -->
        <TextBlock Grid.Row="1" 
                   x:Name="VersionText"
                   Text="" 
                   FontSize="13"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,20"/>
        
        <!-- Description -->
        <TextBlock Grid.Row="2"
                   x:Name="DescriptionText"
                   Text="Loading..." 
                   FontSize="13"
                   TextWrapping="Wrap"
                   HorizontalAlignment="Center"
                   TextAlignment="Center"
                   Margin="0,0,0,15"/>
        
        <!-- Developer Info -->
        <StackPanel Grid.Row="3" HorizontalAlignment="Center" Margin="0,0,0,10">
            <TextBlock HorizontalAlignment="Center" Margin="0,0,0,5">
                <Run Text="Originally developed by " FontSize="12"/>
                <Hyperlink NavigateUri="https://github.com/Bukinnear/TimeTrack" 
                           RequestNavigate="Hyperlink_RequestNavigate"
                           FontSize="12">
                    <Run Text="Jared Kinnear"/>
                </Hyperlink>
            </TextBlock>
            <TextBlock HorizontalAlignment="Center" Margin="0,0,0,5">
                <Run Text="v2 maintained by " FontSize="12"/>
                <Hyperlink NavigateUri="https://github.com/ozzygit/TimeTrack" 
                           RequestNavigate="Hyperlink_RequestNavigate"
                           FontSize="12">
                    <Run Text="Richard Moore"/>
                </Hyperlink>
            </TextBlock>
        </StackPanel>
        
        <!-- Copyright -->
        <TextBlock Grid.Row="4"
                   Text="&#169; 2020 - 2025" 
                   FontSize="11" 
                   Foreground="#605E5C"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,8"/>
        
        <!-- Technical Info with Icons -->
        <StackPanel Grid.Row="5" HorizontalAlignment="Center" Orientation="Horizontal" Margin="0,0,0,15">
            <!-- C# -->
            <Border Background="#68217A" CornerRadius="4" Padding="8,4" Margin="0,0,6,0">
                <TextBlock>
                    <Run Text="C#" FontSize="11" FontWeight="SemiBold" Foreground="White"/>
                </TextBlock>
            </Border>
            
            <!-- .NET 8 -->
            <Border Background="#512BD4" CornerRadius="4" Padding="8,4" Margin="0,0,6,0">
                <TextBlock>
                    <Run Text=".NET 8" FontSize="11" FontWeight="SemiBold" Foreground="White"/>
                </TextBlock>
            </Border>
            
            <!-- WPF -->
            <Border Background="#0078D4" CornerRadius="4" Padding="8,4" Margin="0,0,6,0">
                <TextBlock>
                    <Run Text="WPF" FontSize="11" FontWeight="SemiBold" Foreground="White"/>
                </TextBlock>
            </Border>
            
            <!-- SQLite -->
            <Border Background="#003B57" CornerRadius="4" Padding="8,4">
                <TextBlock>
                    <Run Text="&#128195; SQLite" FontSize="11" FontWeight="SemiBold" Foreground="White"/>
                </TextBlock>
            </Border>
        </StackPanel>
        
        <!-- Spacer -->
        <Border Grid.Row="6"/>
        
        <!-- Database Location -->
        <StackPanel Grid.Row="7" Margin="0,0,0,15">
            <TextBlock Text="Database Location:" 
                       FontSize="11" 
                       Foreground="#605E5C"
                       Margin="0,0,0,5"/>
            <TextBlock TextWrapping="Wrap">
                <Hyperlink x:Name="DatabaseLocationLink" 
                           Click="DatabaseLocationLink_Click"
                           ToolTip="Click to open database folder in File Explorer">
                    <Run x:Name="DatabaseLocationText" 
                         Text="Loading..." 
                         FontFamily="Consolas" 
                         FontSize="10"/>
                </Hyperlink>
            </TextBlock>
        </StackPanel>
        
        <!-- Close Button -->
        <Button Grid.Row="8" 
                Content="Close" 
                Width="80" 
                Height="32"
                HorizontalAlignment="Center"
                Click="CloseButton_Click"
                IsDefault="True"
                IsCancel="True">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="FontFamily" Value="Segoe UI"/>
                    <Setter Property="FontSize" Value="13"/>
                    <Setter Property="Background" Value="#0078D4"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Cursor" Value="Hand"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        CornerRadius="4"
                                        Padding="12,6">
                                    <ContentPresenter HorizontalAlignment="Center" 
                                                    VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#106EBE"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="#005A9E"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>
    </Grid>
</Window>
