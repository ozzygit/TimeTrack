<Window x:Class="TimeTrack.Views.Dialogs.EditEntryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TimeTrack.Data"
        mc:Ignorable="d"
        Title="Edit Entry - TimeTrack v3" Height="480" Width="500"
        MinHeight="450" MinWidth="450"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource WindowBackground}"
        ResizeMode="CanResize">

    <Window.Resources>
        <local:TimeEntryUIConverter x:Key="TimeEntryUIConverter"/>
        
        <!-- Modern Button Style -->
        <Style TargetType="Button">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Background" Value="{DynamicResource BackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="MinWidth" Value="100"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource ButtonHoverBrush}"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource ButtonHoverBorderBrush}"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="{DynamicResource ButtonPressedBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Primary Button Style -->
        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{DynamicResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource PrimaryHoverBrush}"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="{DynamicResource PrimaryPressedBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Label Style -->
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>

        <!-- Modern TextBox Style -->
        <Style TargetType="TextBox">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="4,3"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="{DynamicResource BackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                        Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="{DynamicResource TextBoxHoverBorderBrush}"/>
                </Trigger>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Card Style -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource BackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" Opacity="0.05" 
                                    BlurRadius="8" ShadowDepth="2" Direction="270"/>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Style="{StaticResource CardStyle}" Margin="12">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Margin="0,0,0,12">
                <TextBlock Text="Edit Time Entry" 
                         FontSize="20" 
                         FontWeight="SemiBold"
                         Foreground="{DynamicResource TextBrush}"
                         Margin="0,0,0,4"/>
                <TextBlock Text="Modify the details of this time entry" 
                         FontSize="12"
                         Foreground="{DynamicResource SubtleBrush}"/>
            </StackPanel>

            <!-- Scrollable Content Area -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,0,0,12">
                <StackPanel>
                    <!-- Start Time and End Time in Two Columns -->
                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Start Time -->
                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Start Time" Style="{StaticResource LabelStyle}"/>
                        <TextBox Grid.Row="1" Grid.Column="0"
                                 x:Name="StartTimeBox"
                                 Text="{Binding StartTime, Converter={StaticResource TimeEntryUIConverter}}"
                                 ToolTip="Enter start time (e.g., 9:00 AM)"/>

                        <!-- End Time -->
                        <TextBlock Grid.Row="0" Grid.Column="2" Text="End Time" Style="{StaticResource LabelStyle}"/>
                        <TextBox Grid.Row="1" Grid.Column="2"
                                 x:Name="EndTimeBox"
                                 Text="{Binding EndTime, Converter={StaticResource TimeEntryUIConverter}}"
                                 ToolTip="Enter end time (e.g., 5:00 PM)"/>
                    </Grid>

                    <!-- Two Column Layout for Ticket and Recorded -->
                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Ticket Number -->
                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Ticket Number" Style="{StaticResource LabelStyle}"/>
                        <TextBox Grid.Row="1" Grid.Column="0"
                                 Text="{Binding TicketNumber, UpdateSourceTrigger=PropertyChanged}"
                                 ToolTip="Enter ticket or case number"/>

                        <!-- Recorded Checkbox -->
                        <StackPanel Grid.Row="0" Grid.Column="2" Grid.RowSpan="2" VerticalAlignment="Center">
                            <CheckBox IsChecked="{Binding Recorded, UpdateSourceTrigger=PropertyChanged}"
                                      FontFamily="Segoe UI"
                                      FontSize="13"
                                      VerticalAlignment="Center">
                                <TextBlock Text="Mark as Recorded" 
                                         FontFamily="Segoe UI" 
                                         FontSize="13"
                                         Foreground="{DynamicResource TextBrush}"/>
                            </CheckBox>
                        </StackPanel>
                    </Grid>

                    <!-- Notes Section -->
                    <StackPanel>
                        <TextBlock Text="Notes" Style="{StaticResource LabelStyle}"/>
                        <Grid Margin="0,0,0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="8"/>
                                <ColumnDefinition Width="28"/>
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="NotesBox"
                                     Grid.Column="0"
                                     MinHeight="80"
                                     MaxHeight="120"
                                     TextWrapping="Wrap"
                                     AcceptsReturn="True" 
                                     VerticalScrollBarVisibility="Auto"
                                     SpellCheck.IsEnabled="True"
                                     Text="{Binding Notes, UpdateSourceTrigger=PropertyChanged}"
                                     TextChanged="NotesBox_TextChanged"
                                     ToolTip="Enter detailed notes about this time entry"/>
                            <Button Grid.Column="2"
                                    VerticalAlignment="Stretch"
                                    Content="↗" FontSize="12" Padding="0"
                                    MinWidth="0"
                                    Click="BtnNotesPopOut_Click"
                                    ToolTip="Open full notes editor"/>
                        </Grid>
                        <TextBlock x:Name="NotesCharacterCount"
                                   Text="0 characters"
                                   FontSize="11"
                                   Foreground="{DynamicResource SubtleBrush}"
                                   HorizontalAlignment="Right"
                                   Margin="0,0,0,6"/>

                        <!-- Helper Text -->
                        <TextBlock Margin="0,0,0,0"
                                 FontSize="11"
                                 Foreground="{DynamicResource SubtleBrush}"
                                 TextWrapping="Wrap">
                            <Run Text="Tip: Press"/>
                            <Run FontWeight="SemiBold" Text="Ctrl+Enter"/>
                            <Run Text="to save, or"/>
                            <Run FontWeight="SemiBold" Text="Esc"/>
                            <Run Text="to cancel."/>
                        </TextBlock>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>

            <!-- Buttons - Fixed at Bottom -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="1" 
                        Content="Save" 
                        Style="{StaticResource PrimaryButton}"
                        IsDefault="True" 
                        Click="OnSaveClick"
                        Margin="0,0,8,0"
                        ToolTip="Save changes (Ctrl+Enter)"/>

                <Button Grid.Column="2" 
                        Content="Cancel" 
                        IsCancel="True"
                        ToolTip="Discard changes (Esc)"/>
            </Grid>
        </Grid>
    </Border>
</Window>
